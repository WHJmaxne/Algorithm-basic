using System;
using System.Collections.Generic;

namespace _01ArrayList
{
    internal class Program
    {
        private static void Main(string[] args)
        {

            //LoopArrayList persons = new LoopArrayList();
            //for (int i = 1; i < 31; i++)
            //{
            //    persons.Add(i);
            //}

            //List<string> s = new List<string>();
            //for (int i = 0; i < 15; i++)
            //{
            //    persons.Move(8);
            //    s.Add(persons.CurrentValue.ToString());
            //    persons.RemoveCurrNode();
            //}

            //Console.WriteLine(persons.ToString());

            // TwoWayArrayList l = new TwoWayArrayList();
            // for (int i = 1; i < 11; i++)
            // {
            //     l.Add(i);
            // }
            // Console.WriteLine(l.ToString());
            // Console.WriteLine(l.CurrentValue);
            // l.RemoveCurrNode();
            // Console.WriteLine(l.CurrentValue);
            // Console.WriteLine(l.ToString());
            // l.AfterMove(20);
            // Console.WriteLine(l.CurrentValue);
            // Console.WriteLine(l.ToString());

            // l.BeforeMove(13);
            // Console.WriteLine(l.CurrentValue);
            // Console.WriteLine(l.ToString());
        }
    }
}